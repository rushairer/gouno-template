# Contributing

Use Go 1.25 or newer. Run `bash scripts/verify-template.sh` before opening a pull request. The repository is a rendered-project template, so Go tooling must run against its generated temporary project rather than the placeholder-containing root.

Use Conventional Commit PR titles and add user-visible changes to `CHANGELOG.md` under `Unreleased`.
